{
  "name": "rent-a-chair",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev": "docker-compose up -d && cd apps/web && npm run dev",
    "build": "docker-compose build",
    "down": "docker-compose down"
  }
}